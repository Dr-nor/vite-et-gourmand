{% extends 'base.html.twig' %}

{% block title %}Gestion Employés — Admin{% endblock %}

{% block content %}
<section class="py-5">
    <div class="container">
        <div class="d-flex justify-content-between align-items-center mb-4">
            <h1 class="fw-bold" style="color: #1A6BAD;">👥 Gestion des Employés</h1>
            <a href="{{ path('app_admin_dashboard') }}"
               class="btn btn-outline-secondary">← Dashboard</a>
        </div>
        <div class="alert alert-warning">
            Accès réservé à ROLE_ADMIN uniquement.
        </div>
    </div>
</section>
{% endblock %}
